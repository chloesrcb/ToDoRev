//Page A Faire

include layout.jade
 
body(id="home-page")
  header
      include navbar.jade
  table
    thead
      tr
        each colonne in colonneList
          th #{colonne.dataValues.LibColonne}
    tbody
      each ligne in ligneList
        tr  
          - for(var i = 0; i < ligneList.length; i++) {
          td #{ligneList[i].dataValues.LibLigne}
          - for(var j = 0; j < colonneList.length; j++){
              td #{caseList[i][j].dataValues.estCoche}
          - }
          - }

  

  .container
    form.form-inline(action="/matiere/#{nomMatiere}/" method='post') 
      .form-group
        label(for="ajout")   
        input(type="text" name='contenu' class="form-control" placeholder="Ajouter une tâche") 
        button(type="submit" class="btn btn-info") +

  footer
    include footer.jade
